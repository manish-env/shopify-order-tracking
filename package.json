{
  "name": "shopify-tracking-api",
  "version": "1.0.0",
  "description": "A production-ready Cloudflare Worker API for Shopify order tracking logic",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "start": "wrangler dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dayjs": "^1.11.10"
  },
  "devDependencies": {
    "wrangler": "^3.78.12"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": [
    "shopify",
    "tracking",
    "api",
    "orders",
    "cloudflare",
    "worker"
  ],
  "author": "",
  "license": "MIT"
}
